#include <stdio.h>

typedef struct{
  int elem[10];
  int inicio; /* posição de início*/
  int quantidade_elementos;
}estrutura_fila;

inserir_elemento(estrutura_fila *fila){
  if((*fila).quantidade_elementos == 10){
    printf("A fila está cheia, não há como inserir.\n");
  }
  else{
    *fila.inicio
  }
}


int main(){
  estrutura_fila fila;
  fila.elem[0] = 32;
  fila.elem[1] = 33;
  fila.elem[2] = 34;
  fila.elem[3] = 35;
  fila.elem[4] = 36;
  fila.inicio = 0;
  fila.quantidade_elementos = 5;


  return 0;
}
